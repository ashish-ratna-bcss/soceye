/**
 * Platform monitors feature boundary.
 * Export stable utilities only — do not re-export full page modules.
 */
export { proxyInstagramMediaUrl, buildInstagramMediaCandidates } from '@/shared/utils/mediaProxy';
